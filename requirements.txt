# Livecode Python dependencies (Python >= 3.10; developed on 3.11).
# Core server (livecode.py / livecode_server.py):
websockets>=13,<14

# Catalog + show tooling (assets/tools/*, autoplay.py, set_play.py, ...):
requests>=2.31

# Autopilot host, Playwright regression test, streaming (autopilot_host.py,
# test_steps.py, stream.py). After install run:  playwright install chromium
playwright>=1.45

# stream.py Twitch keys via .env (optional — only needed for streaming):
python-dotenv>=1.0
