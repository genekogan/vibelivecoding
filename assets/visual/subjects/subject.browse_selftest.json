{
  "format": "livecode-visual-v1",
  "id": "subject.browse_selftest",
  "kind": "subject",
  "name": "Browse Selftest",
  "desc": "Bouncing beat-synced circle that proves browse.html renders assets correctly.",
  "tags": [
    "selftest",
    "circle",
    "test",
    "bounce"
  ],
  "slots": [
    "subA"
  ],
  "budget": "light",
  "params": {
    "hue": {
      "default": 140,
      "min": 0,
      "max": 360,
      "doc": "circle hue"
    },
    "energy": {
      "default": 1,
      "min": 0,
      "max": 2,
      "doc": "bounce + pulse amount"
    },
    "speed": {
      "default": 1,
      "min": 0.25,
      "max": 4,
      "doc": "bounce rate in beats"
    },
    "scale": {
      "default": 1,
      "min": 0.2,
      "max": 3,
      "doc": "overall size"
    },
    "x": {
      "default": 0.5,
      "min": 0,
      "max": 1,
      "doc": "horizontal position 0..1"
    },
    "y": {
      "default": 0.6,
      "min": 0,
      "max": 1,
      "doc": "vertical position 0..1"
    },
    "style": {
      "default": "flat",
      "options": [
        "flat",
        "ring"
      ],
      "doc": "solid fill or outline ring"
    },
    "seed": {
      "default": 0,
      "min": 0,
      "max": 9999,
      "doc": "variation seed (shifts hue)"
    }
  },
  "verified": {
    "at": "2026-07-10T19:00:06+00:00",
    "fps": 62.9,
    "motion": 0.0125,
    "lum": 0.095
  },
  "code": "const D = {hue:140, energy:1, speed:1, scale:1, x:0.5, y:0.6, style:'flat', seed:0};\nconst P = Object.assign({}, D, (window.state.P && window.state.P.__SLOT__) || {});\nconst S = window.state.__SLOT__ || (window.state.__SLOT__ = {});\nconst K = window.state.clk || {t:0, beat:0, pulse:0, swell:0};\nconst A = window.state.audio || {bass:0, rms:0};\nconst h = (P.hue + P.seed * 0.036) % 360;\nconst r = 60 * P.scale * (1 + 0.5 * P.energy * K.pulse + 0.4 * A.bass);\nconst bob = Math.sin(K.beat * Math.PI * P.speed + P.seed) * 40 * P.energy;\nconst cx = width * P.x, cy = height * P.y + bob;\nif (P.style === 'ring') { noFill(); stroke(h, 80, 95); strokeWeight(4 + 6 * K.pulse); }\nelse { noStroke(); fill(h, 80, 95); }\ncircle(cx, cy, r * 2);"
}
